global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

# Add service scrape targets with their health/metrics endpoints as each phase implements them.
