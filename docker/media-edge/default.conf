server {
    listen 8080;
    server_name _;

    location = /health {
        default_type text/plain;
        return 200 "ok\n";
    }

    # Playback routes stay disabled until G6 adds signed/authorized manifest delivery.
    location /media/ {
        return 501;
    }
}
